[project]
name = "chat-saude"
version = "0.1.0"
requires-python = ">=3.11"

[project.optional-dependencies]
dev = [
  "ruff>=0.6",
  "pytest>=8.0",
]

[tool.ruff]
line-length = 200
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I"]
